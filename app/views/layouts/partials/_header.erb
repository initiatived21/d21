<%= react_component('PageHeader', ReactHelper.settings({
    controller: controller_name,
    action: action_name,
    currentUser: ActiveModelSerializers::SerializableResource.new(current_user).as_json,
    authToken: form_authenticity_token
  },
  params,
  { class: 'c-page-head' }))
%>
