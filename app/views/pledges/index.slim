== react_component('Search', ReactHelper.settings({\
  pledges: @pledges,
  query: @query,
  filter: @filter,
  resultIds: @result_ids,
  resultCount: @result_count \
}, params, class: 'c-base-layout__main'))
